# 步 4 · 校验

**本步仅细节档执行**;骨架档跳过(骨架档质检由步 6 组装执行者自查,见 `GOVERNANCE_RULES` 的 `business-rule.md` 三"两档的流程差异")。

机械类。总则见 [`README.md`](./README.md)。

## 范围

对 raw 核格式(编号两段合法、列齐、链路坐标齐)+ **临时编号映射表双向核对**(见规则三·五);换人核(不由步 3 执行者做)。

## 产出

通过 / 不通过清单,**附映射表核对明细**(缺失映射的 F 号清单 + 零引用的 `Dxx-xx`/`Mxx` 清单)。

## 注意

不通过回步 3;本步不改任何内容。

## 通过判据

正向:每个步 2 F 号在映射表中出现,且登记的去处在 raw 正文里真实存在。
反向:raw 正文每条 `Dxx-xx`/`Mxx` 至少被一个 F 号引用。
两项全过 = 通过;任一项不过 = 不通过,回步 3。

## 引用的规则条款

- 硬规则 3(质检只核格式 + 出口去处完整性)(`GOVERNANCE_RULES` 的 `assembly-rules.md` 二.3)
- 出口临时编号机制(`GOVERNANCE_RULES` 的 `assembly-rules.md` 三·五)
