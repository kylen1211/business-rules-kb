# sum-svg-diagram · CHANGELOG

## 作业流程五步扩六步:新增"画前内容清单"+"画后内容核对"两道一致性核对关卡 + 新增通用占位资产 `assets/placeholder-pending.svg`(不涉及母版版本)(2026-08-25)

- 来源:voice-agent kb 画图机制改造(用户拍板终版设计),两处改动均由该项目产品节点落地并同步登记本 CHANGELOG。
- **背景**:此前 F5/F6(私有零件不成卡/异常旁路不连主线)两条判据靠画图执行者临场记忆识别,`images/dual-brain-overview.svg` 实测出现过两处误用(该并入调用方卡片的节点单独成卡、该判"不连主线"的异常旁路接了主链路),事后才被发现,没有前置/收尾两道机械核对关卡兜底。
- **作业流程改为六步**:原五步流程里插入新的第2步"画前内容清单"(原步骤顺延)——画之前先从事实源列出全部候选节点/组件全集(职责摘要+出处),逐项按 F1-F6/A1-A2 分类判断是否单独成卡、是否连主线,同时记录链路内顺序依据与跨链路因果依据的原文出处;原第4步"渲染自查"(现第5步)扩展为"渲染自查 + 画后内容核对",画完拿第2步清单逐项唱名核对是否落在自己名下的卡片上(F5 命中节点例外)、做孤卡反查、连线逐条对照顺序/因果依据。复杂全局图(全局业务图/业务全貌图/全局架构图)建议换人做核对这一步,单条链路的局部图可以自查。
- **新增占位资产**:`assets/placeholder-pending.svg`——一张与任何业务无关的通用"占位·待补真图"提示卡,内置固定特征注释 `sum-svg-diagram · 占位待画资产` 供消费方 grep 巡查存量占位图;消费方判"应画"但推迟实际绘制时原样复制该文件到目标正式文件名占位,后补时用真图整份覆盖、不改文件名与页面链接路径。作业流程第6步"交付"补一句指向该资产的用法说明。已核实 XML 良构(`xml.dom.minidom` 解析通过)、`grep -c` 特征注释命中 1 次。
- 本条为规则文字层加法 + 新增一个占位用途的静态资产文件:两份母版文件(`flow-master.svg`/`architecture-master.svg`)、`evals/evals.json`、`scripts/geo_check.py` 均未改动,母版内版本号与本 CHANGELOG 顶部版本号均不递增(与既往文字层改动口径一致)。

## SKILL.md 流程图结构语法节新增 F5/F6 两条节点粒度与连线判据(不涉及母版版本)(2026-08-25)

- 来源:voice-agent kb dual-brain 链路二局部图(`dual-brain-material-flow-gate.svg`)实测中出现两处画法裁决,当时按流程文字临场判定并只记录在该项目 kb log 的个案条目里;用户复盘确认裁决合理后拍板升级为通用判据,补进本 skill,避免下次同类情形再临场判断。
- **F5 私有零件不成卡**:节点无独立触发时机、只被另一节点内部调用取值时,不单独成卡,并入调用方卡片 bullets 带编号提及(实测案例:D06"当前提问基准快照"只被 D05 各分支调用,单独成卡会被误读为"D05 的下一步")。
- **F6 异常旁路不连主线**:节点属本泳道业务但触发入口是全局/框架级异常通道、无固定顺序位置时,卡片留在泳道内但不接主链路连线,bullets 首行写明触发来源(实测案例:D08"慢脑执行失败错误归因"由全局错误路由触发,连上主线等于捏造顺序因果);判据文字同时写明与 F3 回环/回流边(真实主链路语义,要画线)的区别。
- 本条为规则文字层加法:两份母版文件、`evals/evals.json`、`scripts/geo_check.py` 均未改动,母版内版本号与本 CHANGELOG 顶部版本号均不递增(与既往文字层改动口径一致)。

## SKILL.md 新增"SVG XML 安全"节,收编原分居 kb 侧的 XML 转义/注释安全规则(不涉及母版版本)(2026-08-24)

- 背景:voice-agent kb 侧原先把"XML 注释禁三连短横/箭头符号""文本 &/</> 必须转义"这条规则单独放在自己的 `rules/diagram-rules.md`(理由是"不属于版式/配色范畴,不由本 skill 管辖"),画图执行者因此要同时读两份文档(本 skill 的 SKILL.md + 消费方 kb 的规则文件)才能拿全画法+安全要求,用户实测中指出这是结构性风险("容易顾此失彼"),要求评估收敛方向。
- 判断:该规则是通用 SVG 产出正确性问题(XML 语法合法性),不含任何项目业务语义,对本 skill 的全部消费方(不止 voice-agent 一个项目)都适用——按 Mode B"画法上收到 skill、消费方只留业务语义"的既定分工,理应上收,不该留在单个消费方仓库里。
- 落地:`SKILL.md` 新增独立小节"SVG XML 安全(铁律,不可跳过)",内容与原 kb 规则等价(注释禁三连短横/箭头符号、`&`/`<`/`>` 三字符转义要求),表述去掉项目专属措辞、改为面向全部消费方;同时在作业流程第4步"渲染自查"里补一句显式指向该小节,让这条检查成为第4步(已声明的"感知交付门,不可跳过")的一部分,而不是需要额外记住去读的独立文档——从流程结构上收敛"两处必读"的风险,不是仅新增一段文字。
- 消费方联动(不在本 CHANGELOG 管辖范围内,记录于此备查):voice-agent kb 的 `rules/diagram-rules.md` 原"四、SVG XML 安全"节同步改写为指向本节的一行指针,不再复述规则正文,该项改动记录在 voice-agent 仓库自己的 kb log,不在本 CHANGELOG 重复。
- 本条为纯规则文字层加法:`assets/flow-master.svg`、`assets/architecture-master.svg` 两份母版文件、`evals/evals.json`、`scripts/geo_check.py` 均未改动,母版内版本号与本 CHANGELOG 顶部版本号均不递增(与既往文字层改动口径一致)。

## SKILL.md 结构语法节补芯片避让规则 + 新增几何自检脚本(不涉及母版版本)(2026-08-24)

- 背景:上一条"结构语法节文字精修"记录的真实内容实测(voice-agent 全局业务流程图)交付后,用户已采用并替换进项目正式位;实测过程中额外发现两处可优化项,用户拍板都做,本条记录落地结果。
- `SKILL.md` "通用走线附则"节新增"芯片避让"一条:跨泳道/跨层连线的入口出口点与沿途药丸标签,不得落在目标泳道/层的标签芯片矩形范围内(泳道/层左上角标注名称的圆角小矩形,虽不是内容卡片但同样不可被连线穿过或被药丸遮挡);判据取自实测中真实出现的冲突(F4 拆泳道新增的跨泳道连线入口撞在目标泳道标签芯片上,当时靠手工挑选安全落点绕开)。
- 新增 `scripts/geo_check.py`:第四步"渲染自查"的可选补充闸门,纯 Python 标准库(`xml.etree.ElementTree`/`re`/`sys`),零第三方依赖、零业务词汇。机械核验三类几何铁律:连线不穿卡片、连线端点不落标签芯片、药丸标签之间/与卡片/与标签芯片无包围盒重叠。适用两张母版任一派生图(靠 filter id 命名约定识别卡片/药丸,兼容 flow-master 的 `card-shadow`/`badge-shadow` 与 architecture-master 的 `cardShadow`/`pillShadow` 两套命名)。`SKILL.md` 作业流程第4步补一句何时用、怎么跑。自验:跑通两张母版本身(0 违规,验证脚本对两套 filter 命名与两种画法都兼容)、用本轮实测中真实出现过的违规坐标构造最小夹具(正确复现出 2 处"药丸压卡片"违规)、跑最终已上位到 voice-agent kb 的成品图(0 违规,卡片/药丸/泳道/芯片/连线计数与人工核对一致)。
- 本条为规则文字层加法 + 新增一个纯只读校验脚本文件,`assets/flow-master.svg`、`assets/architecture-master.svg` 两份母版文件、`evals/evals.json`、既有铁律与配色语义均未改动,母版内版本号与本 CHANGELOG 顶部版本号均不递增(与上一条口径一致:版本递增只约束母版几何/配色改动)。

## SKILL.md 结构语法节文字精修(不涉及母版版本)(2026-08-24)

- 依据一次真实内容实测反馈(voice-agent 全局业务流程图画图任务中,F1/F3/F4 三条判据文字在实际内容上暴露出规则空隙),对"结构语法"节 F1/F3/F4 三条的判据与画法文字做精修,其余节、两张母版 SVG、`evals/evals.json`、版本号规则均未改动:
  - F1 条件分支:判据原文只覆盖">2 条"走向,未覆盖"2 条都不是拒绝"(如"无条件路径 + 条件性追加路径")这类边界情况——改为"≥2 条独立路径,且不是经典二元通过/拒绝"的判据,明确均为非拒绝推进路径时不要求条数 >2。
  - F3 重回环:原文只覆盖失败/超时重试这一种反向连线,未覆盖"非失败的成功回流/续接"场景——规则更名为"F3 重回环 / 非失败回流边",判据拆两类场景,画法要求药丸标签按场景措辞区分(重试类沿用"退回步骤 X 重试",非失败回流类须显式带"非失败重试"或等价字样),避免图例笼统写"回环"混淆两类语义。
  - F4 长链折行:原"是否存在语义边界"判据太模糊,未说清"跨业务链路/跨规则页"算不算语义边界——补充强信号判据(步骤是否分属不同业务链路/不同规则页/不同触发时机,命中其一即视为存在语义边界,优先拆泳道),避免因共享同一个宽泛阶段名而误判为无边界、误用泳道内换行。
- 本条为规则文字层精修,`assets/flow-master.svg`、`assets/architecture-master.svg` 两份母版文件本身未改动,母版内版本号与本 CHANGELOG 顶部版本号均不递增。

## SKILL.md 结构语法节新增(不涉及母版版本)(2026-08-24)

- `SKILL.md` 新增"结构语法"节,插入于"作业流程"与"铁律与可调基准"之间,分三子节:流程图结构语法(F1 条件分支 / F2 多链汇聚 / F3 重回环 / F4 长链折行)、架构图结构语法(A1 跨层跳跃连接 / A2 同层组件间关系)、通用走线附则(F2/A4 共用,过道放宽与并行走线间距下限)。均为"走线路由+内容组织"文字规则,复用现有六要素铁律(卡片/连线/药丸/图例),不新增颜色或形状。
- F3 重回环按拍板选定"复用橙色实线(校验链语义)+ 专用药丸标签"方案,未采用"新增第五种回环专用色"的备选方案,母版 `<defs>` 未改动。
- 观察项(本轮不写入 SKILL.md 正文):A3 双向依赖语义细分——现有双箭头单线+双药丸表达是否会引起误读尚未经真实案例验证,待有实测证据再补;A5 架构图层内子分组——与"精简、不要细节"的产品偏好冲突且证据不足,不做,如后续出现同层 5+ 组件必须清晰区隔多个子系统的实测案例再重开。
- 本条为 SKILL.md 规则文字层加法:`assets/flow-master.svg`、`assets/architecture-master.svg` 两份母版文件均未改动,母版内版本号(flow-master v2.0.0 / architecture-master v1.0.0)与本 CHANGELOG 顶部版本号(v1.1.0)均不递增——版本递增规则只约束母版几何/配色改动,不覆盖规则文字层。

## v1.1.0 (2026-08-24)

- `assets/flow-master.svg` 整体换底稿重铸(母版内版本号同步升到 v2.0.0):v1.0.0 的竖版菱形流程图骨架系从错误底稿通用化而来,不是本 skill 应有的形态,予以整体替换,不保留竖版菱形形态。
- 新底稿(唯一正确母版)通用化自 voice-agent 项目「场景路由与会话装配」图定稿:浅色底,多条泳道横带纵向堆叠(圆角虚线彩色容器+左上角彩色标签芯片),泳道内步骤卡片自左向右横排(头部编号来源芯片+粗标题+bullet 要点行),判定语义写进卡片 bullets 与连线药丸标签(不再画菱形),拒绝出口是玫红卡片放在所属泳道右端,连线三类语义(主链路蓝实线/校验链橙实线/拒绝路径红虚线)+药丸文字标签,跨泳道衔接用带药丸标签的折线,底部 💡 一句话核心提示行+横排图例。规模取 3 泳道 × 2~3 步骤卡片,足以示范全部版式词汇表且不冗余。
- 同步范围:`SKILL.md` 选母版对照表与"铁律"节中所有描述流程图形态的措辞(菱形判定→bullets/药丸标签语义,配色系统按架构图/流程图分述,连线语义按架构图/流程图分述);`evals/evals.json` eval-1(用户注册流程图)断言整套改写以匹配新形态,并顺带修复历史断言缺陷:eval-2 的"虚线或明确标注"二选一歧义措辞改为唯一判据(必须虚线),eval-1 玫红色系断言补量化色号,eval-1 原第4条(位置/色系/线型三合一)拆成三条独立断言,eval-2 补一条"按金额走不同审批链"的分支语义覆盖断言。`architecture-master.svg` 与其对应 eval 断言本轮不动。
- `description` 触发词策略本轮不动;其中失实的形态描述"判定菱形"已同轮修正为"右置拒绝出口"(仅形态措辞修正,不构成触发词策略变更)。

## v1.0.0 (2026-08-24)

- 初版。两张骨架母版定稿:
  - `assets/flow-master.svg`(业务流程图:双泳道示例骨架,含判定菱形/右置拒绝出口/图例);
  - `assets/architecture-master.svg`(系统架构图:三层带示例骨架,含结构化组件卡片/语义连线/分层摘要面板/图例)。
- 母版由实际项目实例(2026-08-24 手工打磨定稿)通用化而来,业务内容已全部替换为占位文字。
